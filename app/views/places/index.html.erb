<h3>places#index</h3>
<h1>Main page where people will SWIPE places</h1>

++++++++++++++++++++++++++++++++
<p>total of open places: <%= @places.size %></p>
++++++++++++++++++++++++++++++++


<% @places.each do |place|  %>

  <% if place.google_data["opening_hours"] %>
    <% if place.google_data["opening_hours"]["open_now"] %>

      <div>

        <br>---- <big><strong>
        <%= link_to place.name, place_path(place)  %>
        -- <i class="fas fa-hand-holding-heart"></i> --> <em><small>
        <%= link_to "match it", place_matches_path(place, {:place => {:place_id =>  place.id }}), :action => :create, :method => :post %></small></em>
        </strong></big><br>


      </div>
    <% end %>
  <% end %>

<% end %>
